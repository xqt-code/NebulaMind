spring:
  datasource:
    url: jdbc:mysql://localhost:3306/nebulamind_kb?useSSL=false&serverTimezone=Asia/Shanghai&characterEncoding=utf8&allowPublicKeyRetrieval=true
    username: root
    password: a123456
    driver-class-name: com.mysql.cj.jdbc.Driver
  data:
    redis:
      host: localhost
      port: 6379
      database: 0
      lettuce:
        pool:
          max-active: 8
          max-idle: 8
          min-idle: 0

flyway:
  enabled: true
  baseline-on-migrate: true
  locations: classpath:db/migration

knife4j:
  enabled: true

logging:
  level:

minio:
  endpoint: http://47.106.21.219:9000
  access-key: root
  secret-key: WR123456!
  bucket: nebulamind